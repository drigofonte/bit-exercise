---
description: 'A Feature component.'
labels: ['list', 'listitem', 'ui']
---

import { Feature } from './feature';

## React Component for rendering text

A list item that shows a user's feature alongside an icon (e.g. age, hair colour, etc.)

### Component usage
```js
<Feature label="heading" text="feature value" icon="a ReactNode" />
```

### Using props to customize the text

Modify the text to see it change live:
```js live
<Feature text="hello from Feature" />
```
